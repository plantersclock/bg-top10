{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.9.21",
    "nodemon": "^2.0.4"
  },
  "scripts": {
    "start": "node ./server/index.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && yarn build"
  },
  "engines": {
    "node": "10.16.0"
  }
}